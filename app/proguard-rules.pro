# Add project specific ProGuard rules here.
# Sample app currently ships with isMinifyEnabled = false, so R8 does not run for this
# module's release build. These rules only take effect if minification is enabled later.
